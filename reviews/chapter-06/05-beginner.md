# レビュー担当

HTML/CSS・Ruby まで学習した初学者

# 対象

第6章「一覧と詳細でリソースを分ける」

# 総評

一覧から詳細へ移動する意味が分かりやすい。ID を使って 1 件を探す流れも、コードを追えば理解できる。

# 良い点

- `/movies/:id` の `:id` が実際の URL では UUID になる説明が分かりやすい。
- `find_movie` の条件が短く、何を探しているか分かる。
- 存在しない ID の確認 URL があるので試しやすい。
- 登録後に自分が作った映画の詳細へ移動するのは動きとして分かりやすい。

# 改善が必要な点

## must

なし。

## should

- 問題: `halt` という単語が初めて出るため、処理を止めてレスポンスを返すという意味をもう少し説明してほしい。
- 理由: `return` との違いが気になる読者がいる。
- 修正案: 「`halt` はここで Sinatra の処理を止め、指定したステータスコードと本文を返す」と説明する。

## could

- 改善案: `params["id"]` がどこから来るかをもう一度短く書いてもよい。

# 疑問点

なし。

# 判定

軽微な補足後に次へ進める。

# 修正記録

# 対象

- 第2章「Sinatra をはじめる」
- 第2章終了時点の `app.rb` と実行環境
- 図 2-1

# 対応した指摘

## 公開版から作業用リポジトリへ入る手順を追加

- 指摘元: FBC ベテランメンター、初学者。技術書編集者と FBC 卒業生の疑問点
- 対応: `fjordllc/sinatra-textbook-ja` を clone し、`chapter-01` タグから `chapter-02-work` ブランチを作り、`pwd` でリポジトリのルートを確認する節を追加しました。
- 理由: 公開版だけを読んでいる人も、FBC 固有の案内なしに `.ruby-version`、`Gemfile`、`Gemfile.lock` がある開始状態を用意できるようにするためです。
- 変更したファイル: `manuscript/part1/chapter2.md`、`docs/chapter-designs/chapter-02.md`
- 検証結果: 公開リポジトリを新しい一時ディレクトリへ clone し、本文どおり `chapter-01` から `chapter-02-work` を作成しました。その時点には `app.rb` がなく、`.ruby-version`、`Gemfile`、`Gemfile.lock` があることを確認しました。

## 最初に起動してから Puma、Rack、Sinatra を説明する順序へ変更

- 指摘元: 技術書編集者、FBC 卒業生
- 対応: バージョンと Gem の準備後、`app.rb` の作成、初回起動、200 の観察を続け、その後に構成要素の役割を説明する順へ移しました。
- 理由: 起動ログに現れた Puma と、実際に書いた Sinatra のコードを見てから名前と役割を結び付けるためです。
- 変更したファイル: `manuscript/part1/chapter2.md`、`docs/chapter-designs/chapter-02.md`
- 検証結果: 節番号が 2.1 から 2.11 まで重複なく並び、初回起動が構成説明より前にあることを確認しました。

## Rack を実行主体ではなく境界として図示

- 指摘元: Ruby・Sinatra・Rack 技術者、フロントエンドエンジニア、初学者
- 対応: ブラウザ、Puma、Sinatra だけを実行主体の箱として残し、Rack は Puma と Sinatra の間にある破線の「共通インターフェース」として描き直しました。図を縦方向にし、リクエストを下向き、レスポンスを上向きに示しました。
- 理由: Rack を別サーバーや別プロセスとして理解させず、狭い画面でも図中文字を読めるようにするためです。
- 変更したファイル: `scripts/figures/build_all.py`、`manuscript/assets/fig-2-1.svg`、`manuscript/part1/chapter2.md`
- 検証結果: `xmllint` に成功しました。PNG プレビューと mdBook の 500 px 幅表示で、境界、矢印、ラベルが欠けず、文字が読めることを確認しました。

## Bundler 4.0.16 の用意と実出力を明記

- 指摘元: 技術書編集者、Ruby・Sinatra・Rack 技術者、初学者
- 対応: 異なる版の場合は `gem install bundler -v 4.0.16` で用意し、リポジトリルートで `bundle -v` を再確認する手順を追加しました。実際の Bundler 4.0.16 は `4.0.16` とだけ表示するため、「出力に 4.0.16 が含まれる」としました。
- 理由: 確認後の行動を明確にし、実出力と異なる固定文字列を正常条件にしないためです。
- 変更したファイル: `manuscript/part1/chapter2.md`
- 検証結果: 固定した Ruby 環境で `bundle -v` が `4.0.16` を返すことを確認しました。

## rackup の役割を具体化

- 指摘元: Ruby・Sinatra・Rack 技術者
- 対応: rackup は、本書の起動方法で Sinatra が Puma を起動するために使うサーバーハンドラーを提供すると説明しました。
- 理由: 「Rack の起動機能」では Rack 本体との違いと、何を起動するかが曖昧だったためです。
- 変更したファイル: `manuscript/part1/chapter2.md`
- 検証結果: Sinatra 4.2.1 の `Rackup::Handler.pick` と固定環境の起動表示を再確認しました。

## 操作ミスと起動エラーを短く切り分けられるように修正

- 指摘元: FBC ベテランメンター、初学者
- 対応: `get "/movies"` を残して `get "/"` だけを置き換えると明記しました。`Address already in use` の場合は前のアプリが動いていないか確認し、詳細を付録 D へ送る案内も追加しました。
- 理由: 初回起動と最初のコード変更で頻出する、進行不能につながる操作ミスを最小限の説明で防ぐためです。
- 変更したファイル: `manuscript/part1/chapter2.md`
- 検証結果: 付録 D への内部リンクが解決することをリンク検査で確認しました。

## リダイレクトの観察結果を言語化する確認を追加

- 指摘元: FBC ベテランメンター、FBC 卒業生
- 対応: 「ブラウザ」「302 のレスポンス」「二つ目の GET」を使って流れを一文で説明する確認を、Network パネルの観察直後へ追加しました。
- 理由: 独立した演習を増やさず、`redirect` が別ルートを直接呼ぶのではないことを読者自身の言葉で確認するためです。
- 変更したファイル: `manuscript/part1/chapter2.md`
- 検証結果: 302、`Location`、二つ目の GET、200 の説明の後、完成コード確認の前に配置されていることを確認しました。

## 狭幅で対応関係が欠けない構成へ修正

- 指摘元: 再検証で発見。フロントエンドレビューの狭幅確認を適用
- 対応: Network の順序を番号付きリスト、完成コードとレスポンスの対応を箇条書きへ変更しました。初回の列削減後も長いインラインコードで右端が欠けたため、表自体を使わない構成にしました。
- 理由: 500 px 幅の全章スクリーンショットで、完成コードの表が横方向へはみ出していたためです。
- 変更したファイル: `manuscript/part1/chapter2.md`
- 検証結果: 生成 HTML で二つの箇所が `li` 要素になり、横幅を固定する表が残っていないことを確認しました。本文、コード、図版は 500 px 幅の全章表示で確認済みです。

## 章末見出しと次章予告を具体化

- 指摘元: 技術書編集者、Ruby・Sinatra・Rack 技術者
- 対応: 最後の節を「この章のコードを確認する」とし、次章で `Content-Type` を Network パネルから確認すると予告しました。
- 理由: Network 観察との役割の重複を避け、文字列から HTML 文書へ進む観察対象を明確にするためです。
- 変更したファイル: `manuscript/part1/chapter2.md`

## DSL の英語へ学習の焦点が移らないよう補足

- 指摘元: 技術書編集者
- 対応: 英語の展開より、`get` が HTTP の GET と Ruby の処理を結ぶ点を押さえるよう追記しました。再検証で強調記号がそのまま表示される問題を見つけ、閉じ記号の後へ空白を追加しました。
- 理由: 用語の暗記ではなく本章の学習ゴールへ焦点を戻し、Markdown を正しく表示するためです。
- 変更したファイル: `manuscript/part1/chapter2.md`

# 採用しなかった指摘

## 独立した演習としてリダイレクトを説明させる

- 関係する指摘: Network パネルの観察結果を言語化する提案
- 理由: FBC Press と本書の方針どおり、独立した部末演習や機械的な章末演習は設けません。観察した直後の本文へ短い確認として組み込みました。

## 将来の表も短い列名にするという本章での変更

- 指摘元: フロントエンドエンジニア
- 理由: 将来の章のレビュー項目として維持します。本章の表は、実際の狭幅表示で判明したはみ出しに対して列数そのものを減らしました。

# 新たな設計判断

## 外部リンク検査で見つけた前章のリンク切れも修正する

- 決定: 第2章の外部リンク検査で 404 になった第1章の RFC 9110、RFC 9112 を、RFC Editor が現在 200 を返す公式 info ページへ更新しました。
- 理由: 章タグは履歴として維持しますが、`main` は最新の完了章時点で既知のリンク切れがない状態にするためです。
- 影響する章やファイル: `manuscript/part1/chapter1.md`

## 第2章は `chapter-01` タグから作業を始める

- 決定: 公開版の読者は `main` ではなく、前章の完了タグから作業ブランチを作ります。
- 理由: `main` は最新の完了章へ進むため、全章公開後も本文どおりの開始状態を再現できるようにするためです。
- 影響する章やファイル: 今後の章冒頭で前章タグを毎回案内するかは、第3章の章設計で重複量を見て判断します。

## 公開リポジトリ URL を clone 手順の正本とする

- 決定: 公開先は `https://github.com/fjordllc/sinatra-textbook-ja` とします。
- 現在の状態: 2026 年 7 月 17 日に公開リポジトリを作成し、第1章完了時点の `main` と `chapter-01` タグをプッシュしました。本文の3コマンドを別ディレクトリで実行し、第2章の開始状態を確認しました。
- 次の作業: 第2章の完了判定後に、完了コミットと `chapter-02` タグをプッシュします。

## Rack の図は実行主体とインターフェースを同じ形にしない

- 決定: 実行主体は実線の箱、共通インターフェースは破線の境界として描き分けます。
- 理由: 図だけを見ても、Rack が独立プロセスではないことを判別できるようにするためです。

# 残る課題

- 再レビューで `must` の解決と新たな矛盾がないことを確認する。
- 第2章の完了コミットと `chapter-02` タグを公開リポジトリへプッシュする。

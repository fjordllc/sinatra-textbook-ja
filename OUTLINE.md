# FBC Press: Sinatra 詳細目次

この本は、Sinatra そのものを網羅する本ではありません。Sinatra を使って、Web アプリケーションの基礎、HTTP、リクエストとレスポンス、CRUD、ファイル保存、PRG、XSS、404、デバッグを学ぶ教科書です。

本編では、ログインなし・単一ユーザー・ローカル環境で動く小さな Web アプリケーション「映画図鑑」を章ごとに育てます。映画図鑑は、映画のタイトル、監督、公開年、ジャンル、紹介文を登録し、一覧表示・詳細表示・編集・削除できるアプリです。最後には、ルーティング、ERB テンプレート、JSON の読み書き、フォーム、エスケープ、起動方法まで確認できる完成形にします。

映画図鑑は、一覧・詳細・登録・編集・削除を自然に学べ、複数の属性を持つ分かりやすい題材として使います。Web アプリケーションとして自然な形を優先し、標準的な実装、完成コード、CRUD、PRG、XSS 対策、404、入力チェックを不自然に省略しません。

本書は OSS として公開する技術書なので、本文は FBC の課題を知らなくても自然に読める内容にします。FBC 固有の提出方法や提出課題そのものの解説は本文に入れません。

## 想定読者と前提知識

本書は、次の読者を対象にします。

- Ruby の基本文法を学習済みである。
- HTML と CSS の基礎を学習済みである。
- Git と GitHub の基本操作ができる。
- Web アプリケーション開発と Rails は未経験である。

REST の詳しい理論は扱いませんが、FBC 外の読者にも単独で読めるよう、リソース、URI、HTTP メソッドと CRUD の対応は実装に必要な範囲で説明します。REST の事前学習は必須としません。

## 執筆開始時の技術前提

2026 年 7 月 17 日時点では、次の構成を基準にします。原稿とサンプルコードを作り始める前に、この組み合わせで起動と主要機能を検証し、リポジトリ内で固定します。

- Ruby 4.0.6 を `.ruby-version` で固定する。
- Sinatra は 4.2 系を `Gemfile` に明記し、`Gemfile.lock` で実際のバージョンを固定する。
- Sinatra のクラシックスタイル（Classic Style）を利用する。
- Web サーバーには Puma を利用する。
- 起動コマンドは `bundle exec ruby app.rb` に統一する。
- コマンド例には原則として `bundle exec` を付ける。
- `enable :method_override` をコードに明記し、既定値へ暗黙に依存しない。

参考 URL は執筆時に実在確認のうえ、各章末の `## 参考資料` に掲載します。各章には、その章から先へ進むための `## さらに学ぶ` も置きます。記号の凡例は、◎が一次情報の核、○が補助資料です。

## 確認と演習の方針

本書では、独立した部末演習を設けません。FBC には本書とは別に Sinatra の提出課題があり、Web アプリケーションを自力で設計・実装する実践の場が用意されています。本書に別の大きな演習を加えると役割が重複し、どちらも中途半端になる可能性があるためです。

一方で、観察、試行、言語化は重視します。Network タブを見る、フォームを送信する、入力値を変える、再読み込みする、短い変更を加えるといった確認を、必要な章や節の流れへ自然に組み込みます。「確認しよう」「考えてみよう」「少し変更してみよう」を毎章同じ形で並べず、その内容を理解するために必要な場所へ置きます。

---

# 第1部 Web アプリケーションの入口

第1部では、読者がこれから作るものを「Ruby のプログラム」ではなく「ブラウザから HTTP で利用するアプリケーション」として捉え直します。完成形へ急がず、ブラウザ、サーバー、URL、リクエスト、レスポンスの関係を小さく動かして確認します。

## 第1章 Web アプリケーションはどこで動いているのか

**学習ゴール:** ブラウザ、サーバー、リクエスト、レスポンスの関係を図と Network タブの情報を使って説明できる。

- 1.1 ブラウザだけでは映画図鑑は動かない
- 1.2 URL はファイル名ではなくリクエストの宛先
- 1.3 リクエストとレスポンスの往復
- 1.4 HTTP のメッセージをテキストとして読む
- 1.5 静的な HTML と Web アプリケーションの違い
- 1.6 本書で作る映画図鑑の範囲
- 1.7 Network タブで最初に見るもの
- さらに学ぶ: HTTP のバージョンと、ブラウザ・サーバー間の通信
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP>
- ◎ <https://developer.mozilla.org/ja/docs/Learn/Common_questions/Web_mechanics/What_is_a_web_server>
- ◎ <https://www.rfc-editor.org/rfc/rfc9112.html>

### 章設計シート

- 主張: Web アプリケーションは画面そのものではなく、ブラウザからのリクエストにサーバーがレスポンスを返す仕組みである、なぜなら同じ URL へのアクセスでもサーバー側の処理によって返る内容が決まるから。
- 読者が読後にできる判断: 「HTML ファイルを開く」ことと「ローカルサーバーへアクセスする」ことを区別できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. ブラウザで HTML ファイルを直接開く例。
  2. ローカルサーバーにアクセスして同じように HTML を見る例。
  3. Network タブで URL、メソッド、ステータスコード、Request Headers、Response Headers、レスポンス本文を確認する例。
- 技術上の注記: 本文では、メソッド、パス、ヘッダー、本文からなる HTTP/1.1 のメッセージ例を使って説明する。HTTP/2 以降は通信時の内部表現が異なることを短い注記で補い、本文の学習範囲は広げない。
- 先回りして潰す誤解: Web アプリケーションはブラウザ内だけで完結している、という誤解を 1.2 と 1.3 で潰す。
- 前章から受けるもの: なし。Ruby、HTML/CSS、Git/GitHub を学習済みという前提だけを置く。
- 次章へ渡すもの: サーバーがリクエストを受けてレスポンスを返すという見取り図。
- この章にしか書けない固有の中身: 映画図鑑を作り始める前に、ブラウザと Sinatra アプリの境界を明確にすること。

## 第2章 Sinatra をはじめる

**学習ゴール:** Sinatra アプリを起動し、URL とルーティングの対応を追いながら最小のレスポンスを返せる。

- 2.1 Sinatra は Ruby でレスポンスを書くための道具
- 2.2 `.ruby-version`、`Gemfile`、`Gemfile.lock` の役割
- 2.3 Bundler と Puma を含む開発環境
- 2.4 Sinatra のクラシックスタイルと Rack の関係
- 2.5 最小の `app.rb`
- 2.6 `bundle exec ruby app.rb` で起動する
- 2.7 `GET /` に文字列を返す
- 2.8 `GET /` から `/movies` へのリダイレクト
- 2.9 `GET /movies` で映画図鑑の入口を作る
- 2.10 Network タブで `GET /` から `GET /movies` への移動を見る
- さらに学ぶ: Rack が Web サーバーと Sinatra をつなぐ役割
- ◎ <https://sinatrarb.com/>
- ◎ <https://github.com/sinatra/sinatra>
- ◎ <https://bundler.io/>
- ◎ <https://rack.github.io/>

### 章設計シート

- 主張: Sinatra はルーティングごとにレスポンスを返す Ruby の小さな Web アプリケーションである、なぜなら `get "/movies"` のような定義が HTTP リクエストと Ruby の処理を直接結びつけるから。
- 読者が読後にできる判断: どの URL にアクセスすると、どの Ruby コードが実行されるかを追える。
- 積み上げる具体例 / 対比 / 根拠:
  1. `get "/"` で文字列を返す最小例。
  2. `get "/movies"` を追加し、URL ごとに返る内容が変わる例。
  3. `redirect "/movies"` を使い、レスポンスが別 URL への移動を指示する例。
- 先回りして潰す誤解: リダイレクトは画面表示の一種である、という誤解を 2.8 と 2.10 で潰す。ここでは PRG ではなく「別の URL へ移動させるレスポンス」として扱う。
- 前章から受けるもの: ブラウザとサーバーのリクエスト / レスポンスの関係。
- 次章へ渡すもの: Ruby で返した文字列ではなく、HTML をレスポンスとして返す準備。
- この章にしか書けない固有の中身: `GET /` → リダイレクト → `GET /movies` を Network タブで観察し、後の PRG の前提を作ること。

---

# 第2部 画面表示とフォーム

第2部では、映画図鑑を HTML の画面として育てます。最初は静的な表示から始め、ERB、フォーム、POST、`params` の順に進みます。ここではまだ JSON 保存を急がず、ブラウザから送られた値が Sinatra に届くことを観察します。

## 第3章 HTML をレスポンスとして返す

**学習ゴール:** Ruby の配列とハッシュを ERB で HTML に埋め込み、レイアウトを使った映画一覧を表示できる。

- 3.1 文字列のレスポンスと HTML のレスポンス
- 3.2 `views/` と ERB テンプレート
- 3.3 `layout.erb` が共通の HTML 構造を持つ理由
- 3.4 映画一覧の静的な HTML
- 3.5 Ruby の配列とハッシュから映画を表示する
- 3.6 映画は最初から 5 つの属性を持つ
- 3.7 一覧には全項目を出さない理由
- 3.8 `public/` に置くものと置かないもの
- 3.9 最小限の CSS
- さらに学ぶ: ERB、レイアウト、静的ファイル配信
- ◎ <https://sinatrarb.com/intro.html>
- ◎ <https://docs.ruby-lang.org/ja/latest/library/erb.html>

### 章設計シート

- 主張: Web アプリケーションの画面は、サーバーが組み立てた HTML レスポンスである、なぜなら ERB で Ruby のデータを HTML に埋め込んだ結果をブラウザが解釈して表示するから。
- 読者が読後にできる判断: `app.rb` の処理、`views/*.erb`、ブラウザに表示される HTML の関係を説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. `get "/movies"` で直接 HTML 文字列を返す例。
  2. `erb :index` へ移し、テンプレートとして表示する例。
  3. `layout.erb` に共通の外枠を置き、各 ERB にはその画面固有の中身だけを書く例。
  4. Ruby の配列から映画のタイトル、公開年、ジャンルだけを一覧に出す例。
- 先回りして潰す誤解: 一覧画面はすべての情報を表示する場所である、という誤解を 3.7 で潰す。詳細画面の意味を残す。
- 前章から受けるもの: URL とルーティングがレスポンスを決めること。
- 次章へ渡すもの: 表示だけでなく、ブラウザから値を送るフォームの必要性。
- この章にしか書けない固有の中身: 映画図鑑の一覧画面で、タイトル・公開年・ジャンルだけを表示する設計判断。

## 第4章 フォームはリクエストを作る

**学習ゴール:** 映画登録フォームを作り、フォームの HTML から送信先、HTTP メソッド、送信されるキーと値を説明できる。

- 4.1 登録画面 `GET /movies/new`
- 4.2 `form` の `action` と `method`
- 4.3 `input`、`select`、`textarea` の名前が `params` のキーになる
- 4.4 映画の登録フォーム
- 4.5 `POST /movies` で送信値を確認する
- 4.6 Network タブで Form Data を見る
- 4.7 HTML フォームが直接送れるメソッドは GET と POST
- さらに学ぶ: フォームデータのエンコードと HTTP リクエスト
- ◎ <https://developer.mozilla.org/ja/docs/Learn/Forms>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTML/Element/form>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Methods/POST>

### 章設計シート

- 主張: フォームは入力欄の集まりではなく、ブラウザに HTTP リクエストを作らせるための HTML である、なぜなら `action`、`method`、各フォーム部品の `name` が送信先と送信内容を決めるから。
- 読者が読後にできる判断: フォームの HTML を見て、どの URI に、どの HTTP メソッドで、どのキーの値が送られるかを読める。
- 積み上げる具体例 / 対比 / 根拠:
  1. タイトルだけの最小フォーム。
  2. 監督、公開年、ジャンル、紹介文を足した映画フォーム。
  3. Network タブと `params.inspect` で送信値を照合する例。
- 先回りして潰す誤解: `textarea` の表示内容や `select` の表示文言がそのまま Ruby の変数名になる、という誤解を 4.3 で潰す。
- 前章から受けるもの: サーバーが HTML を返すこと。
- 次章へ渡すもの: 送られた値を一時確認するだけでなく、アプリのデータとして保存する必要性。
- この章にしか書けない固有の中身: 映画のジャンルに `select`、紹介文に `textarea` を使い、複数のフォーム部品から作られる HTTP リクエストを観察すること。

---

# 第3部 CRUD とファイル保存

第3部では、映画図鑑を CRUD アプリケーションとして育てます。JSON ファイルを 1 つ読み込み、Ruby の配列とハッシュとして操作し、ファイル全体を書き戻します。データベース設計には踏み込まず、ファイル保存でできることと限界の両方を見ます。

## 第5章 JSON ファイルに映画を保存する

**学習ゴール:** フォームから届いた映画データを検証し、UUID を付けて `data/movies.json` へ保存できる。

- 5.1 保存しなければ次のリクエストで消える
- 5.2 `data/movies.json` を作る
- 5.3 JSON を読み込む
- 5.4 UUID で ID を作る
- 5.5 フォームの値を映画データにする
- 5.6 JSON ファイルへ書き戻す
- 5.7 タイトルを必須にする
- 5.8 入力済みの値をフォームに戻す
- 5.9 保存した値を一覧に表示する
- 5.10 登録後の動きを Network タブで見る
- 5.11 この章の完成コード
- さらに学ぶ: JSON、UUID、HTTP リダイレクト
- ◎ <https://docs.ruby-lang.org/ja/latest/library/json.html>
- ◎ <https://docs.ruby-lang.org/ja/latest/library/securerandom.html>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Redirections>

### 章設計シート

- 主張: ファイル保存は、リクエストごとに消える Ruby の値を次のリクエストへ引き継ぐための最小の永続化である、なぜなら JSON ファイルを読み書きすればアプリをまたいで映画データを残せるから。
- 読者が読後にできる判断: `params` から作ったハッシュが、どのように JSON ファイルへ保存され、次の一覧表示で使われるかを追える。
- 積み上げる具体例 / 対比 / 根拠:
  1. `params` を表示するだけでは保存されない例。
  2. `data/movies.json` の空配列を読み込み、映画ハッシュを追加する例。
  3. UUID を ID にして、同じタイトルでも別の映画として保存できる例。
- 先回りして潰す誤解: タイトルや配列の位置を ID にしてよい、という誤解を 5.4 で潰す。HTML の `required` 属性だけで入力チェックは完了する、という誤解を 5.7 と 5.8 で潰す。
- 入力エラー時の振る舞い: タイトルをサーバー側で確認し、保存もリダイレクトもせず、`422 Unprocessable Content` とともに、エラーメッセージと入力済みの全項目を使って登録フォームを再表示する。`required` 属性は補助として利用する。
- 安全な表示の前提: 第5章から保存した利用者入力を一覧とフォームへ表示するため、`Rack::Utils.escape_html` を呼び出す `h` ヘルパーをここで導入する。ブラウザが入力を HTML として解釈する危険は第9章で詳しく扱う。
- リダイレクトの扱い: ここでは PRG という名前を先に教えず、保存後に別の URL へ移動する仕組みとして使う。Network タブで実際のステータスコードと後続の GET を観察し、第8章で設計上の意味を与える。
- 前章から受けるもの: フォームから `params` が届くこと。
- 次章へ渡すもの: 保存された複数の映画から、一覧と詳細を作り、登録後の遷移先を詳細画面へ変える必要性。
- この章にしか書けない固有の中身: `data/movies.json` を 1 ファイルで扱い、利用者入力をファイルパスに埋め込まない設計。

## 第6章 一覧と詳細でリソースを分ける

**学習ゴール:** 映画の集合と 1 件の映画を別の URI で表し、一覧画面と詳細画面を実装できる。

- 6.1 `/movies` は映画の集合を表す
- 6.2 `/movies/:id` は 1 件の映画を表す
- 6.3 ID で映画を探す
- 6.4 詳細画面を表示する
- 6.5 紹介文の改行は CSS で扱う
- 6.6 一覧から詳細へ移動する
- 6.7 登録後は詳細画面へ移動する
- 6.8 存在しない ID を確認する
- 6.9 この章の完成コード
- さらに学ぶ: コレクションと個別リソース、HTML エスケープ
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Methods/GET>
- ○ <https://www.ics.uci.edu/~fielding/pubs/dissertation/rest_arch_style.htm>

### 章設計シート

- 主張: 一覧と詳細を分けるのは画面を増やすためではなく、映画の集合と 1 件の映画を別のリソースとして扱うためである、なぜなら URI が何を指すかを分けると操作の意味が明確になるから。
- 読者が読後にできる判断: `/movies` と `/movies/:id` の違いを、画面構成ではなくリソースの単位で説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. 一覧にタイトル、公開年、ジャンルだけを出す例。
  2. 詳細に監督と紹介文を含めて出す例。
  3. 存在しない ID へのアクセスを、まず `halt 404` で 404 レスポンスにする例。
- 先回りして潰す誤解: 一覧画面にすべて表示すれば詳細画面は不要、という誤解を 6.1 から 6.5 で潰す。
- 安全な表示の前提: 第5章で導入した `h` ヘルパーを、詳細画面で表示する監督や紹介文にも使う。ブラウザが入力を HTML として解釈する危険は第9章で詳しく扱う。
- 前章から受けるもの: ID 付きで映画を保存できること。
- 次章へ渡すもの: 既存の 1 件を変更・削除する操作。
- この章にしか書けない固有の中身: 映画図鑑の複数属性を使って、一覧と詳細の情報量を意図的に変えること。

## 第7章 編集と削除で CRUD を完成させる

**学習ゴール:** PATCH と DELETE の役割を説明し、HTML フォームの制約と method override を使って更新・削除を実装できる。

- 7.1 CRUD はデータへの基本操作
- 7.2 method override を有効にする
- 7.3 編集画面を表示する
- 7.4 編集フォームを作る
- 7.5 更新処理を作る
- 7.6 詳細画面から編集へ進む
- 7.7 詳細画面から削除する
- 7.8 削除処理を作る
- 7.9 Network タブとログで確認する
- 7.10 この章の完成コード
- さらに学ぶ: HTTP メソッドと Rack ミドルウェア
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Methods/PATCH>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Methods/DELETE>
- ◎ <https://rack.github.io/rack/main/Rack/MethodOverride.html>
- ◎ <https://sinatrarb.com/configuration>

### 章設計シート

- 主張: CRUD は画面名の一覧ではなく、リソースに対する作成・読み取り・更新・削除の操作である、なぜなら同じ映画リソースに対して HTTP メソッドを変えることで操作の意味が変わるから。
- 読者が読後にできる判断: `GET /movies/:id/edit` と `PATCH /movies/:id` の役割を分けて説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. 編集フォームを表示する GET。
  2. `_method=patch` を含む POST が、Rack の method override を通過した後に PATCH として扱われる例。
  3. 詳細画面の削除フォームから DELETE を送り、配列から該当映画を取り除く例。
- 先回りして潰す誤解: 編集画面の表示と更新処理は同じ操作である、という誤解を 7.3 と 7.5 で潰す。Network タブに PATCH や DELETE と表示される、という誤解を 7.2 と 7.9 で潰す。
- 観察上の注意: HTML フォームがネットワーク上で送るメソッドは POST である。Chrome の Network タブでは POST と Form Data の `_method` を確認し、Rack を通過した後の PATCH・DELETE は Sinatra のログやルート内の `request.request_method` で確認する。
- 更新エラー時の振る舞い: タイトルが空なら JSON を書き換えず、エラーメッセージと入力済みの全項目を使って編集フォームを再表示する。
- 前章から受けるもの: ID で 1 件の映画を探せること。
- 次章へ渡すもの: 作成・更新・削除後に使ったリダイレクトが、再送信を防ぐ設計としてどのように働くかという問い。
- この章にしか書けない固有の中身: 映画図鑑の編集・削除を通して、HTML フォームの制限と Rack の method override を結びつけること。

---

# 第4部 HTTP の動きを読めるようにする

第4部では、CRUD が動く状態から一歩進み、HTTP の挙動を読めるようにします。Network タブで GET、POST、リダイレクト、ステータスコードを観察し、Sinatra のログと組み合わせて method override 後の PATCH・DELETE まで追います。

## 第8章 リダイレクトは二つのリクエストをつなぐ

**学習ゴール:** 登録・更新・削除後のリクエストを Network タブとログで追い、PRG が再送信を防ぐ理由を説明できる。

- 8.1 先に使っていたリダイレクト
- 8.2 PRG パターンとは
- 8.3 直接 HTML を返すと何が困るのか
- 8.4 登録後の流れを見る
- 8.5 再読み込みで何が起きるか
- 8.6 更新後の流れを見る
- 8.7 削除後の流れを見る
- 8.8 `GET /` のリダイレクトとの違い
- 8.9 この章の完成コード
- さらに学ぶ: HTTP リダイレクトと PRG パターン
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Redirections>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Status/303>

### 章設計シート

- 主張: PRG パターンは登録後の見た目を整えるためではなく、状態を変えるリクエストと表示用の GET を分けるための設計である、なぜなら再読み込み時に POST、PATCH、DELETE が再実行される危険を避けられるから。
- 読者が読後にできる判断: 状態を変える処理の後に、なぜ直接 HTML を返さずリダイレクトするのかを説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. 完成コードを一時的に変更し、POST 後に直接 HTML を返した場合の再読み込み問題を安全なローカル環境で確認する例。
  2. `POST /movies` → `GET /movies/:id` の流れ。
  3. `DELETE /movies/:id` → `GET /movies` の流れ。
- 先回りして潰す誤解: リダイレクトは単に便利な画面遷移である、という誤解を 8.2 から 8.7 で潰す。
- 観察上の注意: 登録では Network タブに POST → 303 → GET が現れる。method override を使う更新・削除では、ブラウザが送る POST と `_method`、303、GET を Network タブで確認し、Sinatra 側で PATCH・DELETE として処理されたことをログで照合する。
- 前章から受けるもの: POST、PATCH、DELETE でデータを変更できること。
- 次章へ渡すもの: 表示する値が利用者入力であることによる危険。
- この章にしか書けない固有の中身: 第2章で見た `GET /` のリダイレクトを、PRG の文脈で再度意味付けすること。

## 第9章 利用者の入力はそのまま HTML にしない

**学習ゴール:** 利用者入力をそのまま HTML にした場合の危険を再現し、表示場所に応じてエスケープできる。

- 9.1 映画データはすべて利用者入力
- 9.2 XSS とは
- 9.3 安全な状態で入力してみる
- 9.4 危険な表示を一時的に作る
- 9.5 属性値で壊れる入力
- 9.6 `textarea` で壊れる入力
- 9.7 `<script>` だけ防げばよいわけではない
- 9.8 映画図鑑で `h` を使う場所
- 9.9 改行表示は CSS で扱う
- 9.10 この章の完成コード
- さらに学ぶ: OWASP の XSS 対策と出力先の文脈
- ◎ <https://owasp.org/www-community/attacks/xss/>
- ◎ <https://developer.mozilla.org/ja/docs/Glossary/Cross-site_scripting>
- ◎ <https://docs.ruby-lang.org/ja/latest/library/cgi.html>
- ◎ <https://docs.ruby-lang.org/ja/latest/library/erb.html>
- ◎ <https://rack.github.io/rack/main/Rack/Utils.html>

### 章設計シート

- 主張: 利用者入力を HTML として扱うと XSS になる、なぜならブラウザはレスポンス内の文字列を、単なる文字ではなく HTML の一部として解釈するから。
- 読者が読後にできる判断: 映画のタイトル、監督、公開年、ジャンル、紹介文を表示するとき、どこでエスケープが必要かを説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. タイトルに `<script>alert("xss")</script>` を入れる例。
  2. 属性値に `"><script>alert("xss")</script>` を入れる例。
  3. `textarea` に `</textarea><script>alert("xss")</script>` を入れる例。
- 先回りして潰す誤解: `<script>` だけ防げばよい、という誤解を 9.4 から 9.7 で潰す。
- 実装方針: `h` ヘルパーは `Rack::Utils.escape_html` を明示的に呼び出す。ERB エンジンの偶然の自動エスケープや、未宣言の依存 Gem には頼らない。
- 前章から受けるもの: 登録・更新後に保存した値を画面へ表示すること。
- 次章へ渡すもの: 不正な URL や存在しない ID をどう扱うか。
- この章にしか書けない固有の中身: 映画図鑑の複数属性と `textarea` を使い、表示場所の文脈によって壊れ方が変わることを確認すること。

## 第10章 見つからないものには 404 を返す

**学習ゴール:** 存在しない URL と映画 ID に 404 を返し、アプリの例外による 500 と区別できる。

- 10.1 404 は「見つからない」というレスポンス
- 10.2 404 ページを作る
- 10.3 存在しない URL を扱う
- 10.4 存在しない映画 ID を扱う
- 10.5 編集・更新・削除でも 404 を返す
- 10.6 404 と 500 の違い
- 10.7 映画図鑑の完成状態を確認する
- 10.8 この章の完成コード
- さらに学ぶ: HTTP のエラーステータスと Sinatra のエラーハンドラー
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Status/404>
- ◎ <https://developer.mozilla.org/ja/docs/Web/HTTP/Status/500>
- ◎ <https://sinatrarb.com/intro.html>

### 章設計シート

- 主張: 404 は見た目のエラーページではなく、要求されたリソースが見つからないことを HTTP レスポンスで伝える仕組みである、なぜならブラウザや利用者はステータスコードと本文の両方から結果を判断するから。
- 読者が読後にできる判断: 存在しない映画 ID とアプリのバグを、404 と 500 の違いとして切り分けられる。
- 積み上げる具体例 / 対比 / 根拠:
  1. 存在しない URL にアクセスする例。
  2. 存在しない UUID の映画詳細にアクセスする例。
  3. 意図的な例外で 500 との差を見る例。
- 先回りして潰す誤解: エラー画面を出せば HTTP ステータスは何でもよい、という誤解を 10.4 と 10.6 で潰す。
- 前章から受けるもの: 利用者入力と URL は信用しすぎないこと。
- 次章へ渡すもの: 思ったとおりに動かないときに、どこから調べるか。
- この章にしか書けない固有の中身: 映画 ID を使った詳細・編集・削除の各入口で、見つからない 1 件をどう扱うか。
- 完成状態の確認: 最終ディレクトリ構成、すべてのルーティング、ERB テンプレート、JSON の読み書き、登録・編集・削除フォーム、タイトル検証、`h` ヘルパー、404 ページ、CSS、`bundle exec ruby app.rb` による起動を一覧で照合する。完成コードの各ファイルを省略せず確認できる状態にする。

---

# 第5部 調べ方と次の学習

第5部では、映画図鑑を完成品として飾るのではなく、学習用アプリとして振り返ります。ログ、例外、Network タブ、ファイル保存の限界を結びつけ、次のデータベース設計へ橋渡しします。

## 第11章 動かないときは境界を見る

**学習ゴール:** ブラウザ、HTTP、Sinatra、ERB、JSON ファイルのどこで問題が起きているかを事実から切り分けられる。

- 11.1 まず再現手順を短くする
- 11.2 ブラウザの表示と Network タブを分けて見る
- 11.3 Sinatra のログを見る
- 11.4 `params` と JSON ファイルを見る
- 11.5 ERB のエラーを読む
- 11.6 `layout.erb` と各 ERB の役割を確認する
- 11.7 症状ごとに最初に見る場所
- 11.8 よくある失敗を切り分ける
- 11.9 付録のエラー集へ進む
- 11.10 この章の完成コード
- さらに学ぶ: ブラウザ開発者ツールと Sinatra のログ
- ◎ <https://developer.mozilla.org/ja/docs/Learn/Common_questions/Tools_and_setup/What_are_browser_developer_tools>
- ◎ <https://sinatrarb.com/intro.html>

### 章設計シート

- 主張: Web アプリケーションのデバッグは原因を当てる作業ではなく、ブラウザ、HTTP、サーバー、テンプレート、保存データの境界で事実を集める作業である、なぜなら同じ「表示されない」でも壊れている場所が違うから。
- 読者が読後にできる判断: 画面が崩れた、保存されない、404 になる、500 になる、といった症状ごとに最初に見る場所を選べる。
- 積み上げる具体例 / 対比 / 根拠:
  1. フォーム送信値が届いていない例。
  2. JSON には保存されているが ERB に表示されていない例。
  3. ID が見つからず 404 になっている例。
- 先回りして潰す誤解: エラーメッセージは最後に読むもの、という誤解を 11.3 と 11.5 で潰す。
- 前章から受けるもの: 404 と 500 を区別すること。
- 次章へ渡すもの: ファイル保存で作ったアプリを、どこまで使えるものとして考えるか。
- この章にしか書けない固有の中身: 映画図鑑の完成に近い状態を使い、画面・HTTP・JSON のどこを見るかを具体的に切り分けること。

## 第12章 ファイル保存の先にデータベースがある

**学習ゴール:** JSON ファイルによる保存の限界を、同時更新、データ量、関連、整合性の観点から説明できる。

- 12.1 映画図鑑でできるようになったこと
- 12.2 1 ファイル全体を読み書きしている
- 12.3 同時に書き込むと壊れる可能性がある
- 12.4 データが増えたときに探しにくい
- 12.5 データ同士の関連を扱いにくい
- 12.6 整合性を守る仕組みが足りない
- 12.7 ER 図や正規化はここでは扱わない
- 12.8 Rails で隠れて見えにくくなるもの
- 12.9 次に学ぶこと
- 12.10 この章の完成コード
- さらに学ぶ: データベースの役割と Active Record の入口
- ◎ <https://www.sqlite.org/whentouse.html>
- ○ <https://guides.rubyonrails.org/active_record_basics.html>

### 章設計シート

- 主張: JSON 保存は Web アプリケーションの流れを学ぶには十分だが、複数人・大量データ・関連・整合性を扱うには限界がある、なぜならファイル全体の読み書きではデータを安全かつ効率よく扱いにくいから。
- 読者が読後にできる判断: 映画図鑑で学んだ CRUD の考え方を保ったまま、なぜ次にデータベース設計を学ぶ必要があるかを説明できる。
- 積み上げる具体例 / 対比 / 根拠:
  1. 1 件更新でも `movies.json` 全体を書き戻す例。
  2. 同時更新で片方の変更が失われる可能性。
  3. 映画と監督、ジャンル、レビューなどの関連を増やしたくなったときの限界。
- 先回りして潰す誤解: JSON 保存は小さなアプリなら本番でも十分、という誤解を 12.2 から 12.5 で潰す。
- 前章から受けるもの: 映画図鑑を動かし、調べられる状態。
- 次章へ渡すもの: 本編はここで閉じ、付録と次のデータベース設計へ進む。
- この章にしか書けない固有の中身: 映画図鑑の `movies.json` で実際に経験した読み書きを、データベース設計の必要性へ接続すること。

---

# 付録

付録は本編を読むうえでは必須ではありません。ただし、自分の Sinatra アプリを公開・整理するときや、チーム・スクールなどの提出物として整えるときに必要になる内容を置きます。

## 付録A README

- ローカル起動手順
- 必要な Ruby バージョン
- `bundle install`
- `bundle exec ruby app.rb` に統一した起動方法
- アプリの概要
- 第三者が `git clone` して起動できる状態の確認
- 本文との差分を README に書きすぎない理由

## 付録B RuboCop

- RuboCop の役割
- 導入と実行
- 指摘を読む
- 自動修正に頼りすぎない
- 提出物として整える前に見る観点

## 付録C ERB Lint

- ERB Lint の役割
- 導入と実行
- ERB と HTML の崩れを見つける
- RuboCop との違い

## 付録D よくあるエラー

- `LoadError`
- `Gemfile` と `bundle exec`
- ルーティングが見つからない
- `params` のキーが想定と違う
- JSON ファイルが壊れた
- 404 と 500 の切り分け
- `public/` に保存データを置いてしまう
- `layout.erb` と各 ERB の両方に `<html>` や `<body>` を書いてしまう
- POST 後に HTML を直接返してしまう
- Network タブを見ずに画面だけで原因を探してしまう
- 紹介文の改行表示を `gsub("\n", "<br>")` で HTML 化してしまう
- 利用者入力をエスケープせず表示してしまう

## 付録E さらに学ぶための資料案内

- 各章の「さらに学ぶ」から参照する資料の索引
- Sinatra 公式
- Rack
- HTTP
- OWASP
- REST
- Rack::Test
- CSRF
- CSV
- データベース

---

# 図版計画

図版は装飾ではなく、文章とコードだけでは追いにくい関係を示すために使います。各図は 1 つの主張に絞ります。

- 第1章: ブラウザから Sinatra へリクエストが届き、レスポンスが戻る往復。
- 第4章: HTML フォームの `action`、`method`、`name` が HTTP リクエストのどこに対応するか。
- 第7章: ブラウザの POST が Rack の method override を通り、Sinatra では PATCH または DELETE として扱われる流れ。
- 第8章: POST → 303 → GET で構成される PRG の時系列。
- 第11章: ブラウザ、HTTP、Sinatra、ERB、JSON ファイルのデバッグ境界。

---

# 映画図鑑の仕様メモ

## カリキュラム上の接続メモ

本書の本文は FBC を知らない読者にも自然に読める OSS 技術書として書きます。ただし設計上は、次の学習順を意識します。

- 本書の前には REST、URI 設計、リソース、『Web を支える技術』の学習がある。
- 本書では REST 理論を深掘りせず、REST を Sinatra で実装することを重視する。
- 本書の次にはデータベース設計、ER 図、リレーション、正規化の学習がある。
- 本書では JSON 保存までに留め、ファイル保存の限界からデータベース設計へつなげる。

## 学習目標

読者が読後に次の状態になることを目指します。

- PRG パターンで解決する問題を説明できる。
- Chrome DevTools の Network タブで GET、POST、リダイレクトを観察し、method override による PATCH・DELETE を Sinatra のログと照合できる。
- Bundler と `bundle exec` を使って開発環境を再現できる。
- README にセットアップと起動方法を書く意味を説明できる。
- HTTP/1.1 の例を使って、HTTP メッセージをメソッド、パス、ヘッダー、本文に分けて読める。
- HTML フォームが直接送信できる HTTP メソッドは GET と POST だけだと説明できる。
- REST を意識して URI を設計できる。
- XSS を理解し、利用者入力を表示するときに基本的なエスケープができる。
- `public/` は静的ファイルを置く場所であり、保存用 JSON を置かない理由を説明できる。
- `layout.erb` と各 ERB テンプレートの役割を分けて説明できる。
- Sinatra が Rack の上で動き、Rails を含むほかの Ruby 製 Web フレームワークにも Rack との接点があることを説明できる。

## 執筆時の判断基準

迷ったときは、次の順で判断します。

1. Web アプリケーション開発を正しく理解できるか。
2. 初学者にとって分かりやすいか。
3. 一般的で再利用可能な実装になっているか。
4. OSS の技術書として単独で成立しているか。
5. FBC の提出課題を直接解説する内容になっていないか。

5 番目を理由に、1〜4 を損なわないようにします。映画図鑑は最後まで動作する完成形にし、標準的な CRUD、JSON 保存、REST を意識した URI、POST・PATCH・DELETE、PRG、XSS 対策、404、入力チェックを扱います。

## データ構造

映画は次の属性を持ちます。

タイトル、監督、公開年、ジャンル、紹介文は、最初の映画データと登録フォームからすべて登場させます。章の途中で属性を追加してデータ構造を変える進め方は採用しません。一覧では一部だけ、詳細ではすべてを表示することで、画面ごとの役割を説明します。

| 属性 | Ruby・JSON 上のキー | 内容 |
| --- | --- | --- |
| ID | `id` | 映画を一意に識別する UUID |
| タイトル | `title` | 映画のタイトル |
| 監督 | `director` | 監督名 |
| 公開年 | `year` | 公開された年 |
| ジャンル | `genre` | 映画のジャンル |
| 紹介文 | `description` | 映画についての短い説明 |

ジャンルは日本語の選択肢として保存します。

- アクション
- コメディ
- ドラマ
- ホラー
- SF
- アニメーション
- その他

## URI と HTTP メソッド

| 用途 | HTTP メソッド | URI |
| --- | --- | --- |
| トップページ | GET | `/` から `/movies` へリダイレクト |
| 一覧表示 | GET | `/movies` |
| 新規登録画面 | GET | `/movies/new` |
| 登録処理 | POST | `/movies` |
| 詳細表示 | GET | `/movies/:id` |
| 編集画面 | GET | `/movies/:id/edit` |
| 更新処理 | PATCH | `/movies/:id` |
| 削除処理 | DELETE | `/movies/:id` |

登録・更新・削除後の遷移は次に統一します。

- `POST /movies` → `303 See Other` → `GET /movies/:id`
- `PATCH /movies/:id` → `303 See Other` → `GET /movies/:id`
- `DELETE /movies/:id` → `303 See Other` → `GET /movies`

HTML フォームによる更新と削除では、ブラウザが送信する実際の HTTP メソッドは POST です。Rack の method override が `_method` の値を読み、Sinatra アプリ内で PATCH または DELETE として扱います。Network タブでは POST と `_method` を確認し、Sinatra のログと組み合わせて処理の全体を追います。

## 入力チェック

- 登録と更新の両方でタイトルを必須とする。
- サーバー側で空文字を確認する。
- HTML の `required` 属性は補助として利用する。
- エラー時は保存せず、リダイレクトもしない。
- エラーメッセージと入力済みの全項目を使ってフォームを再表示する。

## HTML エスケープ

- `Rack::Utils.escape_html` を呼び出す `h` ヘルパーを定義する。
- `title`、`director`、`year`、`genre`、`description` を表示するときに明示的に使う。
- ERB エンジンによる暗黙の自動エスケープには依存しない。
- XSS の脆弱な例は第9章の確認中だけ作り、確認後に安全な完成コードへ戻す。

## 本編で扱わないもの

- ログイン
- ユーザーごとの映画管理
- お気に入り
- 評価や星の数
- レビュー投稿
- コメント
- 検索
- 絞り込み
- 並び替え
- ページネーション
- 画像アップロード
- 映画ポスター
- 外部 API
- 複数リソース間の関連
- データベース
- 同時更新
- 排他制御
- JavaScript 中心の UI
- テスト

# 第2章 再レビュー

# 対象

- 第2章「Sinatra をはじめる」
- 第2章終了時点の `app.rb` と実行環境
- 図 2-1
- `reviews/chapter-02/review-summary.md`
- `reviews/chapter-02/changes.md`

# 総評

初回レビューで統合した二つの `must` は、修正と実地検証まで完了しています。公開版を読む人は、実在する公開リポジトリを clone し、`chapter-01` タグから第2章の開始状態を用意できます。図 2-1 は Rack を独立した箱から破線の境界へ変更し、本文の「共通インターフェース」という説明と一致しました。

初回起動を Puma・Rack・Sinatra の構成説明より前へ移したことで、読者は先に 200 のレスポンスを得てから、起動ログとコードに現れた名前へ役割を与えられます。修正による新たな技術的誤り、進行不能な手順、原稿とコードの不一致は見つかりませんでした。

# `must` の解決確認

## 公開版から作業用リポジトリを用意する手順

- 状態: 解決
- 確認内容: `https://github.com/fjordllc/sinatra-textbook-ja` を公開リポジトリとして作成し、第1章完了時点の `main` と `chapter-01` タグをプッシュしました。別の一時ディレクトリで本文の `git clone`、`cd`、`git switch -c chapter-02-work chapter-01` を実行できました。
- 開始状態: `app.rb` は存在せず、`.ruby-version`、`Gemfile`、`Gemfile.lock` が存在します。本文どおりに最初の `app.rb` を作れる状態です。
- 新たな問題: なし。リポジトリをタグへ切り替えると原稿も当時の状態へ戻るため、読者は公開版の本文を開いたまま作業する前提です。clone 手順を置く本節自体が公開版にあるため、進行は妨げません。

## Rack を独立した実行主体に見せる図

- 状態: 解決
- 確認内容: 実行主体の箱はブラウザ、Puma、Sinatra の三つです。Rack は Puma と Sinatra の間にある破線の共通インターフェースとして描かれています。リクエストとレスポンスの矢印は境界内の説明文を横切りません。
- 狭幅表示: 横長の四箱から縦方向へ変更したため、500 px 幅でも図中文字を読めます。代替テキストと可視キャプションも維持されています。
- 新たな問題: なし

# `should` と `could` の確認

- 初回起動は 2.5、2.6、構成説明は 2.7 となり、先に動かしてから名前を付ける順序になりました。
- Bundler 4.0.16 がない場合のインストール手順が追加され、実際の `bundle -v` 出力に合わせて確認条件が書かれています。
- rackup は、Sinatra が Puma を起動するために使うサーバーハンドラーを提供すると説明されました。
- `get "/movies"` を残し、`get "/"` だけを置き換える範囲が明確です。
- `Address already in use` の最初の確認先と付録 D への導線があります。
- 302 の観察後に、ブラウザが二つ目の GET を送る流れを一文で説明する確認があります。独立した部末演習にはしていません。
- 最後の節は完成コードを確認する役割が見出しから分かり、次章の `Content-Type` 観察へつながります。
- DSL の英語を暗記することより、HTTP と Ruby の対応を理解するよう焦点が示されています。強調記号も生成 HTML で正しく解釈されています。
- 狭幅で欠けた対応表は、番号付きリストと箇条書きへ変更されました。生成 HTML に固定幅の表は残っていません。

# 6 観点からの再確認

## FBC ベテランメンター

Network パネルで 200、302、200 を追い、`Location` と二つ目の GET を説明できます。`bundle exec`、再起動、ポート使用中の切り分けも初学者が実践できる範囲に収まっています。FBC の提出課題固有の説明はありません。

## 技術書編集者

章の目的、環境準備、最初の成功、構成の振り返り、ルート追加、リダイレクト、完成状態という順序が自然です。11節ありますが、各節の役割は重複せず、章末で次章へ渡すものが明確です。

## Ruby・Sinatra・Rack 技術者

Classic Style、ルートの選択、戻り値、302、`Location`、`bundle exec`、Puma、rackup、Rack の説明は固定バージョンの挙動と一致します。未使用の method override、ERB、`config.ru` は追加されていません。

## フロントエンドエンジニア

HTML をまだ実装しない章として境界が守られています。図にはキャプションと代替テキストがあり、Rack の境界も色だけに依存せず破線とラベルで区別できます。狭幅では長い対応関係をリストとして折り返せます。

## 初学者

clone する場所、作業開始タグ、Ruby と Bundler の確認、`app.rb` の作成場所、起動・停止・再起動、確認する URL が順に示されています。途中コードと完成コードがあり、変更範囲も明確です。

## FBC 卒業生の現役 Web エンジニア

HTTP メソッドとパスを処理へ結び付ける見方、リダイレクトを二つのリクエストとして捉える見方は、Rails へ進んだ後にも転用できます。Rack の内部仕様へ広げず、共通インターフェースという橋渡しに留めています。

# 原稿・コード・図版の一致

- 章終了時点として掲載した `app.rb` と、リポジトリ直下の `app.rb` は一致します。
- `Gemfile`、`Gemfile.lock` の Ruby、Sinatra、Puma、rackup、Bundler の版は本文と一致します。
- `bundle exec ruby app.rb` で、本文どおり Puma がポート 4567 で待ち受けます。
- `GET /` は 302 と `Location: http://localhost:4567/movies` を返します。
- `GET /movies` は 200 と UTF-8 の「映画図鑑」を返します。
- 図 2-1 は `scripts/figures/build_all.py` から再生成でき、本文の役割と方向に一致します。

# 検証結果

- `bundle exec ruby -c app.rb`: 成功
- `bundle exec ruby scripts/verify-stack.rb`: method override、303、ERB、HTML エスケープを含む全項目に成功
- `bundle exec ruby app.rb` と `curl`: 302、`Location`、200、本文、二行のサーバーログを確認
- `mdbook build`: 成功
- `scripts/check-links.sh internal`: 成功
- `scripts/check-links.sh web`: 成功
- `scripts/figures/build_all.py`: 成功
- `xmllint --noout manuscript/assets/fig-1-1.svg manuscript/assets/fig-2-1.svg`: 成功
- `git diff --check`: 成功
- Chrome デスクトップ表示: 見出し、本文、コード、表、目次に重大な崩れなし
- Chrome 500 px 幅表示: 本文、コード、図、キャプションに重なりなし。はみ出したルーティング表はリストへ変更し、生成 HTML で確認
- 公開リポジトリの clone と `chapter-01` からのブランチ作成: 成功

# 外部リンク検査で見つけた前章の問題

第1章の RFC Editor 旧 URL が 404 を返したため、現在 200 を返す公式 info ページへ更新しました。修正後の外部リンク検査は成功しています。第1章の内容やコードには変更を加えていません。

# 前後章との接続

- 前章: ブラウザ、リクエスト、レスポンス、URL のパスという概念を、実際の Sinatra ルートと Network パネルへ接続できています。
- 次章: `GET /movies` の文字列を HTML 文書と ERB へ発展させるための起動方法、ルーティング、200 の観察が整っています。`Content-Type` を次の観察対象として予告しています。

# 残る `must`

なし。

# 完了判定

第2章を完了し、第3章の章設計へ進めます。

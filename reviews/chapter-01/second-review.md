# 第1章 再レビュー

# 対象

- 「はじめに」
- 第1部導入
- 第1章「Web アプリケーションはどこで動いているのか」
- 第1章時点のリポジトリ構成
- `reviews/chapter-01/review-summary.md`
- `reviews/chapter-01/changes.md`

# 総評

初回レビューで統合した五つの `must` はすべて解決されています。章の中心だったリクエストとレスポンスの説明順を保ちながら、初学者が Network パネルを実際に開くための前提と手順が補われました。原稿より先行していた `app.rb` も外れ、第1章終了時点のコード状態と本文が一致しています。

修正による新しい技術的誤り、説明順の破綻、表示上の重大な問題は見つかりませんでした。第2章で Sinatra を起動し、`GET /movies` と Ruby の処理を結び付ける前提が整っています。

# `must` の解決確認

## HTTP(S) のページで Network パネルを観察できる手順

- 状態: 解決
- 確認内容: 対象を `http://` または `https://` の本書ページと明記し、`file://` の場合の代替手順を追加しています。DevTools のショートカット、開いた後に再読み込みする理由、UI 表記差も説明されています。
- 新たな問題: なし

## はじめにの未知語

- 状態: 解決
- 確認内容: CRUD、PRG、XSS は、それぞれデータ操作、再送信防止、安全な表示という目的を先に読めます。REST の事前学習は不要と明記され、未説明の `リソース` と `URI` の列挙は外れています。
- 新たな問題: なし

## 第1章の未定義語 `リソース`

- 状態: 解決
- 確認内容: 映画の一覧と一件の映画という具体的な表現へ置き換わりました。ID にも短い定義があります。
- 新たな問題: なし

## 404 の説明

- 状態: 解決
- 確認内容: `404 Not Found` を、サーバーが見つからなかったことを伝えるために返すステータスコードとして説明しています。第10章で明示的に実装する方針とも一致します。
- 新たな問題: なし

## 原稿とサンプルアプリの状態

- 状態: 解決
- 確認内容: `app.rb` と README の起動案内は第1章時点から削除されました。第1章はコードを書かないという本文と一致します。技術前提の検証は `scripts/verify-stack.rb` として分離されています。
- 新たな問題: なし

# `should` と `could` の確認

- 図 1-1 に可視の図番号とキャプションが追加され、代替テキストも維持されています。
- macOS 固有の `file://` 例であることと、OS による差が明記されています。
- URL スキームは `http` と `file` の両方を説明できる表現になりました。
- HTTP メッセージ例が簡略化されていることを、実際の追加ヘッダーに触れて示しています。
- Network パネルで見つけた三項目を、一文で説明する確認が追加されています。
- `Protocol` 列を本章に追加しない判断は妥当です。観察対象を増やさず、HTTP のバージョン差を注記に留める設計を維持できています。

# 原稿・コード・図版の一致

- 原稿は第1章で Sinatra コードを書かないと説明し、リポジトリにも読者用 `app.rb` はありません。
- 図版は `scripts/figures/build_all.py` から `manuscript/assets/fig-1-1.svg` を再生成できます。
- 図中の「ブラウザ」「Web アプリケーション」「リクエスト」「レスポンス」は本文の用語と一致します。
- `manuscript/SUMMARY.md` から第1章と図版への参照が解決されています。
- 章末の参考資料は、本文で使った HTTP、Web サーバー、Chrome Network パネル、HTTP 仕様に対応しています。

# 検証結果

- `mdbook build`: 成功
- `scripts/check-links.sh internal`: 成功
- `scripts/verify-stack.rb`: method override、303、ERB、HTML エスケープの全項目に成功
- `scripts/figures/build_all.py`: 成功
- `xmllint --noout manuscript/assets/fig-1-1.svg`: 成功
- `git diff --check`: 成功
- Chrome デスクトップ表示: 本文、表、図、キャプション、目次に重大な崩れなし
- Chrome 500 px 幅表示: 見出しと本文に横方向の欠落や重なりなし

# 前後章との接続

- 前章: なし。「はじめに」で対象読者、映画図鑑、Network パネルを使う方針が示されています。
- 次章: URL は物理ファイル名とは限らず、ルーティングがパスと処理を結び付けるという理解を渡せます。`app.rb` は第2章で初めて追加できる状態です。

# 残る `must`

なし。

# 完了判定

第1章を完了し、第2章の章設計へ進めます。


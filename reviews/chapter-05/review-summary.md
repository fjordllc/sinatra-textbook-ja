# レビュー統合

## 対象

第5章「JSON ファイルに映画を保存する」

## 総評

6 観点すべてで、章の流れと技術範囲は妥当と判断された。第4章の `params.inspect` から JSON 保存へ進み、UUID、タイトル必須チェック、入力値保持、リダイレクト、`h` ヘルパーまでを扱う構成は、映画図鑑を自然に育てる流れになっている。

未解決の `must` はない。`should` は、初学者が観察しやすくするための補足、JSON 読み書きの対比、`File.join(__dir__, ...)` の意図、エラー表示の最低限の構造、第8章への橋渡しに集約される。

## must

なし。

## should

### JSON ファイルの変化を観察する手順を具体化する

- 該当レビュー: `01-fbc-mentor.md`
- 対応方針: 採用する。
- 理由: 画面だけでなく保存ファイルを見る習慣を付ける章であり、送信前後で何を見るかを明確にする。

### `File.join(__dir__, ...)` の意図を補足する

- 該当レビュー: `02-technical-editor.md`
- 対応方針: 採用する。
- 理由: 起動ディレクトリではなく `app.rb` から見た保存場所を指すためのコードであることを説明する。

### 壊れた JSON やファイル欠落は範囲外だと明記する

- 該当レビュー: `03-ruby-sinatra.md`
- 対応方針: 採用する。
- 理由: 例外処理へ範囲を広げず、付録やデバッグ章へ回す判断を明確にできる。

### エラーメッセージ領域に `role="alert"` を付ける

- 該当レビュー: `04-frontend.md`
- 対応方針: 採用する。
- 理由: 詳しいアクセシビリティ説明はしないが、エラー表示の HTML として自然な最低限の改善である。

### `JSON.parse` と `JSON.pretty_generate` を対比する

- 該当レビュー: `05-beginner.md`
- 対応方針: 採用する。
- 理由: 読み込みと書き込みで使うメソッドを混同しやすいため。

### 第8章の役割を補足する

- 該当レビュー: `06-fbc-graduate.md`
- 対応方針: 採用する。
- 理由: 第5章で 303 を観察できるため、第8章では再送信防止という設計意図を学ぶことを明確にする。

## could

- `JSON.pretty_generate` を使う理由として、ファイルを人間が確認しやすいことを補足する。採用する。
- `select` の初期値が最初の項目になることは、この章の本筋ではないため採用しない。
- `@movie = {}` で存在しないキーを読んでも `nil` になることは、本文が重くなるため採用しない。
- 手編集で JSON 構文エラーが起きることは、デバッグ章で扱うため採用しない。

## 対立した指摘

なし。

## 対応後の確認観点

- 完成コードと実ファイルの内容が一致していること。
- `role="alert"` が本文の完成コードと `views/new.erb` の両方に反映されていること。
- 第5章で `h` ヘルパーを導入したことにより、第6章の章設計と矛盾しないこと。

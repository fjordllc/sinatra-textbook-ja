# 修正記録

## 対応した指摘

### ルート順の重要性を補足する

- 対象レビュー: `01-fbc-mentor.md`
- 対応: 具体的なルートを先に、変化する部分を含むルートを後に書くと追記した。
- 理由: `/movies/new` が `/movies/:id` に誤って吸収されるつまずきを防ぐため。

### CSS の完成コードを追加する

- 対象レビュー: `02-technical-editor.md`
- 対応: 詳細画面用 CSS と狭幅向けメディアクエリを本文に追加した。
- 理由: 実ファイルと原稿を照合できるようにするため。

### `find_movie` の読み込み方の限界を補足する

- 対象レビュー: `03-ruby-sinatra.md`
- 対応: 分かりやすさを優先して毎回 JSON を読み込み、限界は第12章で扱うと追記した。
- 理由: 教材上の簡潔さと実装上の限界を両方示すため。

### 狭幅表示の CSS を補う

- 対象レビュー: `04-frontend.md`
- 対応: `@media (max-width: 600px)` で `.movie-detail div` を 1 列へ切り替える CSS を追加した。
- 理由: スマートフォン幅で詳細画面が窮屈になることを避けるため。

### `halt` の意味を説明する

- 対象レビュー: `05-beginner.md`
- 対応: `halt` は処理を止め、指定したステータスコードと本文を返すと説明した。
- 理由: 初出の Sinatra API を読み飛ばさず理解できるようにするため。

### 次章への橋渡しを追加する

- 対象レビュー: `06-fbc-graduate.md`
- 対応: 章末に、次章では詳細画面から編集・削除へ進むと追記した。
- 理由: ID で 1 件を扱えることが次章の前提になると明確にするため。

## 採用しなかった指摘

### 詳細リンクを「詳細を見る」にする

- 理由: 表の操作列では短い「詳細」で十分意味が伝わり、画面の密度を保てるため。

### `find` の図解を追加する

- 理由: この章ではコードと短い説明で追える範囲であり、図を追加すると焦点が Ruby メソッドの説明へ寄りすぎるため。

### `halt 404` の本文がプレーンテキストであることを補足する

- 理由: 第10章で専用 404 ページを作るため、ここでは 404 ステータスの確認に集中する。

## 新たな設計判断

- 紹介文の改行表示は第6章で必要になるため、`white-space: pre-line` をここで導入する。第9章では XSS と表示文脈の観点から再度扱う。
